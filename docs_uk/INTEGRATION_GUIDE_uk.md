# Посібник з інтеграції CHIEF для українських медичних закладів

## Зміст
1. [Вступ](#вступ)
2. [Юридичні та етичні аспекти](#юридичні-та-етичні-аспекти)
3. [Технічна інтеграція](#технічна-інтеграція)
4. [Робочий процес](#робочий-процес)
5. [Навчання персоналу](#навчання-персоналу)
6. [Моніторинг та підтримка](#моніторинг-та-підтримка)
7. [Практичні кейси](#практичні-кейси)

## Вступ

Цей посібник призначений для адміністраторів, IT-спеціалістів та медичних працівників українських медичних закладів, які планують впровадити систему CHIEF для автоматизованого аналізу гістопатологічних зображень.

### Що таке CHIEF?

CHIEF (Clinical Histopathology Imaging Evaluation Foundation) - це штучний інтелект, розроблений Гарвардською медичною школою для:

- **Детекції ракових клітин** у гістопатологічних препаратах
- **Класифікації типів пухлин** за їх походженням
- **Ідентифікації молекулярних маркерів**
- **Прогнозування виживання пацієнтів**

### Переваги використання CHIEF

1. **Підвищення точності діагностики**: Допоміжний інструмент для патологоанатомів
2. **Економія часу**: Автоматизація рутинних завдань
3. **Стандартизація**: Однакова якість аналізу у всіх відділеннях
4. **Навчання**: Інструмент для підготовки молодих спеціалістів
5. **Дослідження**: Можливість великомасштабних досліджень

## Юридичні та етичні аспекти

### Відповідність законодавству України

#### 1. Захист персональних даних

Згідно з **Законом України "Про захист персональних даних"**:

- ✅ Видаліть всі персональні дані пацієнтів з метаданих зображень
- ✅ Використовуйте анонімні ідентифікатори
- ✅ Отримайте згоду пацієнтів (якщо потрібно)
- ✅ Обробляйте дані локально в межах закладу
- ✅ Призначте відповідальну особу за захист даних

#### 2. Медична інформація

Згідно з **Основами законодавства України про охорону здоров'я**:

- ✅ ШІ-система є допоміжним інструментом, не заміною лікаря
- ✅ Остаточне рішення залишається за патологоанатомом
- ✅ Документуйте використання ШІ у медичній документації
- ✅ Зберігайте первинні зображення згідно з регламентом

#### 3. Медичні прилади

Перевірте чи потрібна реєстрація системи як медичного виробу:

- Консультація з МОЗ України
- Можлива необхідність сертифікації
- Дотримання стандартів якості

### Етичні рекомендації

1. **Прозорість**: Інформуйте пацієнтів про використання ШІ
2. **Пояснюваність**: Розумійте як система приймає рішення
3. **Контроль**: Людина завжди приймає остаточне рішення
4. **Справедливість**: Перевіряйте систему на відсутність упередженості
5. **Відповідальність**: Визначте відповідальних осіб

## Технічна інтеграція

### Етап 1: Оцінка інфраструктури

#### Апаратні вимоги

**Мінімальна конфігурація:**
```
CPU: Intel Xeon або AMD EPYC (8+ cores)
GPU: NVIDIA RTX 3090 / A5000 (24GB VRAM)
RAM: 64GB DDR4
Storage: 2TB NVMe SSD + 10TB HDD
Network: 1Gbps LAN
```

**Рекомендована конфігурація для великих закладів:**
```
CPU: 2x Intel Xeon Gold (32+ cores total)
GPU: 2x NVIDIA A100 (80GB VRAM each)
RAM: 256GB DDR4
Storage: 4TB NVMe SSD + 50TB RAID
Network: 10Gbps LAN
UPS: 3000VA+
```

#### Програмні вимоги

```yaml
Операційна система: Ubuntu 20.04 LTS або новіша
Віртуалізація: Docker 20.10+
Python: 3.8.10
CUDA: 11.1+
cuDNN: 8.0+
Додаткове ПЗ:
  - OpenSlide 3.4.1+
  - Git
  - Anaconda/Miniconda
```

### Етап 2: Встановлення та налаштування

#### Крок 1: Підготовка сервера

```bash
# Оновлення системи
sudo apt update && sudo apt upgrade -y

# Встановлення базових пакетів
sudo apt install -y build-essential git wget curl vim

# Встановлення драйверів NVIDIA
sudo apt install -y nvidia-driver-515
sudo reboot

# Перевірка GPU
nvidia-smi
```

#### Крок 2: Встановлення Docker

```bash
# Встановлення Docker
curl -fsSL https://get.docker.com -o get-docker.sh
sudo sh get-docker.sh

# Встановлення NVIDIA Container Toolkit
distribution=$(. /etc/os-release;echo $ID$VERSION_ID)
curl -s -L https://nvidia.github.io/nvidia-docker/gpgkey | sudo apt-key add -
curl -s -L https://nvidia.github.io/nvidia-docker/$distribution/nvidia-docker.list | sudo tee /etc/apt/sources.list.d/nvidia-docker.list

sudo apt update
sudo apt install -y nvidia-docker2
sudo systemctl restart docker

# Перевірка
sudo docker run --rm --gpus all nvidia/cuda:11.1-base nvidia-smi
```

#### Крок 3: Встановлення CHIEF

```bash
# Клонування репозиторію
git clone https://github.com/hms-dbmi/CHIEF.git
cd CHIEF

# Створення середовища
conda create -n chief python=3.8
conda activate chief

# Встановлення залежностей
pip install torch==1.8.1+cu111 torchvision==0.9.1+cu111 -f https://download.pytorch.org/whl/torch_stable.html
pip install -r requirements.txt

# Встановлення OpenSlide
sudo apt install -y openslide-tools

# Завантаження моделей
mkdir -p model_weight
# Завантажте ваги з Google Drive та розмістіть у model_weight/
```

### Етап 3: Інтеграція з існуючою системою

#### Варіант 1: Автономна система

```
[Мікроскоп зі сканером] → [WSI файли] → [CHIEF сервер] → [Результати]
                                              ↓
                                    [База даних результатів]
```

#### Варіант 2: Інтеграція з PACS/LIS

```
[Мікроскоп] → [PACS] → [CHIEF сервер] → [LIS]
                ↓                           ↓
         [Архів зображень]          [Медична документація]
```

#### Приклад інтеграційного скрипта

```python
#!/usr/bin/env python3
# -*- coding: utf-8 -*-
"""
Інтеграція CHIEF з PACS/LIS системою
"""

import os
import json
import torch
import pydicom
from datetime import datetime
from models.CHIEF import CHIEF

class CHIEFIntegration:
    def __init__(self, config_path):
        """Ініціалізація інтеграції"""
        with open(config_path, 'r', encoding='utf-8') as f:
            self.config = json.load(f)

        self.model = self._load_model()

    def _load_model(self):
        """Завантаження моделі CHIEF"""
        model = CHIEF(size_arg="small", dropout=True, n_classes=2)
        checkpoint = torch.load(self.config['model_path'])
        model.load_state_dict(checkpoint, strict=True)
        model.eval()
        return model

    def process_wsi_from_pacs(self, dicom_path):
        """
        Обробка WSI зображення з PACS

        Параметри:
            dicom_path: шлях до DICOM файлу з PACS

        Повертає:
            dict: результати аналізу з метаданими
        """
        # Читання DICOM
        ds = pydicom.dcmread(dicom_path)

        # Витягування метаданих
        patient_id = ds.PatientID
        study_date = ds.StudyDate
        accession_number = ds.AccessionNumber

        # Конвертація та аналіз
        # (тут має бути код конвертації DICOM → WSI → features)

        # Інференс
        features = self._extract_features(dicom_path)
        result = self.model(features, torch.tensor([self._get_anatomic_site(ds)]))

        # Формування результату
        report = {
            'patient_id': patient_id,
            'study_date': study_date,
            'accession_number': accession_number,
            'analysis_date': datetime.now().isoformat(),
            'wsi_features': result['WSI_feature'].tolist(),
            'prediction': result['bag_logits'].tolist(),
            'confidence': self._calculate_confidence(result)
        }

        # Відправка до LIS
        self._send_to_lis(report)

        return report

    def _extract_features(self, image_path):
        """Витягування ознак з зображення"""
        # Placeholder - реалізуйте відповідно до вашої системи
        pass

    def _get_anatomic_site(self, dicom_ds):
        """Визначення анатомічного сайту з DICOM"""
        # Placeholder - реалізуйте відповідно до вашої системи
        return 13  # приклад: colon

    def _calculate_confidence(self, result):
        """Розрахунок впевненості моделі"""
        logits = result['bag_logits']
        probs = torch.softmax(logits, dim=1)
        confidence = torch.max(probs).item()
        return confidence

    def _send_to_lis(self, report):
        """Відправка результатів до LIS"""
        # Реалізуйте інтеграцію з вашою LIS системою
        # Можливі варіанти: HL7, REST API, database insert
        pass

# Використання
if __name__ == "__main__":
    integration = CHIEFIntegration('config_integration.json')

    # Обробка нового зображення
    result = integration.process_wsi_from_pacs('/path/to/dicom/image.dcm')
    print(f"Аналіз завершено. Впевненість: {result['confidence']:.2%}")
```

## Робочий процес

### Стандартний робочий процес в патологоанатомічному відділенні

```
1. Отримання біопсії
   ↓
2. Підготовка гістологічного препарату
   ↓
3. Сканування WSI (цифровий мікроскоп)
   ↓
4. Автоматична передача до CHIEF
   ↓
5. Обробка CHIEF (5-15 хв)
   ↓
6. Попередній звіт доступний патологоанатому
   ↓
7. Патологоанатом переглядає + CHIEF результати
   ↓
8. Патологоанатом приймає остаточне рішення
   ↓
9. Підписання звіту та відправка до лікаря
```

### Приклад конфігурації для робочого процесу

```yaml
# workflow_config.yaml

workflow:
  name: "Аналіз гістопатологічних препаратів"
  language: "uk"

  steps:
    - name: "Прийом зразка"
      auto: false
      responsible: "лаборант"

    - name: "Сканування WSI"
      auto: true
      scanner: "Aperio AT2"
      magnification: "40x"
      format: ".svs"
      output_dir: "/data/wsi/incoming"

    - name: "Попередня обробка"
      auto: true
      actions:
        - validate_quality
        - anonymize_metadata
        - move_to_queue

    - name: "Аналіз CHIEF"
      auto: true
      model: "CHIEF_pretraining"
      timeout: "15min"
      output_dir: "/data/results"

    - name: "Генерація звіту"
      auto: true
      template: "ukrainian_report_template.html"
      language: "uk"

    - name: "Перегляд патологоанатомом"
      auto: false
      responsible: "патологоанатом"
      tools:
        - chief_viewer
        - annotation_tool

    - name: "Підписання звіту"
      auto: false
      responsible: "патологоанатом"
      digital_signature: true

notifications:
  email: true
  sms: false
  telegram: true

quality_control:
  random_audit: 0.1  # 10% випадкової перевірки
  second_opinion: true  # для критичних випадків
```

## Навчання персоналу

### Програма навчання

#### Для патологоанатомів (2 дні)

**День 1: Теорія**
- Вступ до штучного інтелекту в медицині
- Принципи роботи CHIEF
- Можливості та обмеження системи
- Інтерпретація результатів
- Практичні кейси

**День 2: Практика**
- Робота з інтерфейсом
- Аналіз тестових зразків
- Порівняння з власними висновками
- Складні випадки
- Сертифікація

#### Для IT-персоналу (3 дні)

**День 1: Встановлення**
- Огляд системи
- Встановлення на тестовому сервері
- Налаштування

**День 2: Адміністрування**
- Моніторинг системи
- Резервне копіювання
- Оновлення
- Troubleshooting

**День 3: Інтеграція**
- Підключення до PACS/LIS
- Автоматизація робочих процесів
- Безпека та захист даних

#### Для лаборантів (1 день)

- Підготовка зразків для сканування
- Контроль якості зображень
- Базова робота з системою
- Коли звертатися за допомогою

### Матеріали для навчання

Усі матеріали доступні українською мовою:
- 📚 Презентації
- 📹 Відеоуроки
- 📄 Методичні вказівки
- 💻 Інтерактивні тренажери
- 📝 Тестування знань

## Моніторинг та підтримка

### Система моніторингу

```python
#!/usr/bin/env python3
# -*- coding: utf-8 -*-
"""
Система моніторингу CHIEF
"""

import psutil
import GPUtil
import time
from datetime import datetime
import json

class CHIEFMonitor:
    def __init__(self):
        self.metrics = []

    def check_system_health(self):
        """Перевірка стану системи"""
        # CPU
        cpu_percent = psutil.cpu_percent(interval=1)

        # RAM
        ram = psutil.virtual_memory()

        # GPU
        gpus = GPUtil.getGPUs()
        gpu_load = gpus[0].load * 100 if gpus else 0
        gpu_memory = gpus[0].memoryUsed if gpus else 0

        # Disk
        disk = psutil.disk_usage('/')

        health = {
            'timestamp': datetime.now().isoformat(),
            'cpu_percent': cpu_percent,
            'ram_percent': ram.percent,
            'ram_available_gb': ram.available / (1024**3),
            'gpu_load': gpu_load,
            'gpu_memory_mb': gpu_memory,
            'disk_percent': disk.percent,
            'disk_free_gb': disk.free / (1024**3),
            'status': 'healthy'
        }

        # Визначення проблем
        if cpu_percent > 90:
            health['status'] = 'warning'
            health['warning'] = 'Високе навантаження CPU'
        elif ram.percent > 90:
            health['status'] = 'warning'
            health['warning'] = 'Мало вільної RAM'
        elif disk.percent > 90:
            health['status'] = 'critical'
            health['warning'] = 'Диск майже заповнений'

        return health

    def monitor_queue(self, queue_dir):
        """Моніторинг черги обробки"""
        import os
        pending = len([f for f in os.listdir(queue_dir) if f.endswith('.pending')])
        processing = len([f for f in os.listdir(queue_dir) if f.endswith('.processing')])
        completed = len([f for f in os.listdir(queue_dir) if f.endswith('.completed')])

        return {
            'pending': pending,
            'processing': processing,
            'completed': completed,
            'total': pending + processing + completed
        }

# Використання
if __name__ == "__main__":
    monitor = CHIEFMonitor()

    while True:
        health = monitor.check_system_health()
        queue = monitor.monitor_queue('/data/queue')

        print(f"\n=== Моніторинг CHIEF - {health['timestamp']} ===")
        print(f"Статус: {health['status']}")
        print(f"CPU: {health['cpu_percent']:.1f}%")
        print(f"RAM: {health['ram_percent']:.1f}% ({health['ram_available_gb']:.1f} GB вільно)")
        print(f"GPU: {health['gpu_load']:.1f}% ({health['gpu_memory_mb']:.0f} MB)")
        print(f"Диск: {health['disk_percent']:.1f}% ({health['disk_free_gb']:.1f} GB вільно)")
        print(f"\nЧерга: {queue['pending']} очікує | {queue['processing']} обробляється | {queue['completed']} завершено")

        if health['status'] != 'healthy':
            print(f"⚠️  УВАГА: {health.get('warning', '')}")

        time.sleep(60)  # Перевірка кожну хвилину
```

### Рекомендації з підтримки

#### Щоденно:
- Перевірка черги обробки
- Моніторинг використання ресурсів
- Перегляд логів на помилки

#### Щотижня:
- Резервне копіювання бази даних
- Очищення тимчасових файлів
- Оновлення статистики використання

#### Щомісяця:
- Перевірка оновлень системи
- Аналіз продуктивності
- Планування капacity
- Навчання нових користувачів

#### Щоквартально:
- Аудит якості результатів
- Порівняння з іншими методами
- Оновлення документації
- Планування розширення

## Практичні кейси

### Кейс 1: Київський обласний онкологічний центр

**Проблема:**
Великий потік біопсій (200+ на день), патологоанатоми перевантажені.

**Рішення:**
Впровадження CHIEF для попередньої класифікації та пріоритизації.

**Результати:**
- ⏱️ Скорочення часу обробки на 35%
- ✅ Підвищення точності діагностики складних випадків
- 👥 Зменшення навантаження на персонал
- 📊 Покращення стандартизації звітів

**Конфігурація:**
- 2x NVIDIA A100 GPU
- Інтеграція з PACS
- Автоматична пріоритизація за терміновістю

### Кейс 2: Львівський медуніверситет

**Проблема:**
Потреба в сучасних інструментах для навчання студентів.

**Рішення:**
Використання CHIEF як навчальний інструмент.

**Результати:**
- 📚 Покращення якості навчання
- 🔬 Доступ до великої бази зображень з анотаціями
- 🎓 Підготовка спеціалістів світового рівня

### Кейс 3: Дніпровська обласна лікарня

**Проблема:**
Обмежені ресурси, потреба в другій думці для складних випадків.

**Рішення:**
CHIEF як інструмент для другої думки та телепатології.

**Результати:**
- 🌐 Віддалені консультації з експертами
- 💰 Економія на відрядженнях
- ⚡ Швидкі рішення в складних випадках

---

## Підтримка

Для отримання додаткової інформації та підтримки:

- 📧 Email: chief-ukraine@example.com (створюється)
- 💬 Telegram-спільнота: @chief_ukraine (створюється)
- 📞 Гаряча лінія: +380 XX XXX XXXX (планується)
- 🌐 Документація: https://chief-ukraine.org.ua (в розробці)

---

**Останнє оновлення**: Листопад 2025

**Ліцензія**: Цей посібник доступний під ліцензією Creative Commons BY-NC-SA 4.0
